;;; beacon.el --- nunca perca o cursor de vista -*- lexical-binding: t; -*-
;;; Commentary:
;; Quando rolar a janela a linha onde o cursor está irá brilhar.

;;; Code:

(use-package beacon
  :config (beacon-mode 1))

(provide 'beacon)
;;; beacon.el ends here
